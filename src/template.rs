use crate::runner;
use crate::util::DynResult;

runner!();

type ParsedData = ();
fn parse(input: &str) -> DynResult<ParsedData> {
    Ok(())
}

fn part1(data: &ParsedData) -> u64 {
    0
}
fn part2(data: &ParsedData) -> u64 {
    0
}
